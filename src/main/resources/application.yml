spring:
  datasource:
    master:
      url: jdbc:postgresql://localhost:5433/social_network
      username: postgres
      password: postgres
    slave:
      url: jdbc:postgresql://localhost:5434/social_network
      username: postgres
      password: postgres
  flyway:
    enabled: false
    locations: classpath:db/migration
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080
